# Python Example of DryCI test caching

To see this project in action:

1. Create a venv (`uv sync && . .venv/bin/activate`)
2. Obtain a DryCI token, and export it as `DRYCI_TOKEN`
3. Run `pytest -sv --dryci tests`
